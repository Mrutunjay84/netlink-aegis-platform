"""Netlink Aegis - AI-assisted Policy builder app (Phase 2).

This proprietary overlay app lives entirely under ``netlink/backend`` and is
injected into the image at Docker build time. It does not modify any community
(``backend/``) source, so upstream merges stay clean.
"""
